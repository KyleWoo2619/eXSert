using UnityEngine;
using UnityEngine.UI;
using TMPro;
public class NavigationLogManager : MonoBehaviour
{
    [SerializeField] private TMP_Text selectedLogTitle;
    [SerializeField] private TMP_Text selectedLogDescription;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
